# 067 — Place Pending decisions in Home and their owning Space

> _This was generated by AI during triage._

## Parent

#33

## What to build

Make every Pending decision easy to find and resolve without forcing the user to search through all
Surfaces. Home shows one compact, expandable global strip summarizing the total pending count. Each
Space card shows a distinct count for decisions owned by that Space. Inside a Space route, show only
that Space's pending-decision notifications above its Surfaces.

Each expanded notification exposes the decision's allowed quick actions and a `Review` action.
Quick actions resolve the exact Pending-decision id through the common daemon contract, including
L2 trust decisions; they never reproduce workflow logic in the shell. `Review` navigates and focuses
the matching Decision Surface, where the user can inspect full context, edit fields when supported,
or choose “approve like this from now on” when an L1 Approval card permits it.

Derive Space ownership from the validated Pending-decision/Surface relationship and synchronize
arrival, resolution, expiry, stale refusal, failure, dismissal, and Surface archival. A decision
whose owning Surface or Space is not yet present remains globally unassigned and is reconciled when
the snapshot catches up; it is never attributed to an unrelated Space.

Preserve decision presentation in chat, while #98 owns natural-language selection and resolution.

## Architectural boundary

The strip, summary, counts, quick actions, and `Review` navigation are fixed-shell components.
Quick actions are alternative channels to the same daemon-owned decision, not duplicate decisions
or client-side implementations of trust, Tree proposal, Space proposal, or update rules.

The validated Decision Surface remains the complete review/edit experience. A shell quick action
uses the prepared payload unchanged and cannot create an allowlist rule.

## Acceptance criteria

- [ ] Home shows one compact global summary with the correct total pending count and an accessible
      expand/collapse control.
- [ ] Each decision contributes exactly once to the owning Space's count; unassigned decisions
      remain visible globally without false ownership.
- [ ] Opening a Space shows only its Pending-decision notifications above its Surfaces.
- [ ] `Review` navigates to and focuses the exact Decision Surface from Home, Space, and chat
      notification presentations.
- [ ] Expanded notifications expose every allowed quick resolution with an accessible, specific
      label and invoke the exact common Pending-decision id.
- [ ] Quick actions work for L1, L2, and every other registered decision kind, preserve prepared
      input, and never create an allowlist rule.
- [ ] Arrival, resolution, failure, expiry, stale refusal, dismissal, Surface archival, reconnect,
      and snapshot reconciliation update strip, counts, Space view, and chat consistently.
- [ ] Repeated clicks and UI/chat races resolve at most once and render the authoritative outcome
      from #97.
- [ ] Existing server-backed Space attention remains a separate signal.
- [ ] Focused ownership, lifecycle, navigation, accessibility, race, and rendering tests plus
      `pnpm check` pass.

## Blocked by

- #66
- #96
- #97

## Out of scope

- Natural-language decision selection and ambiguity handling (#98).
- Editing prepared decision fields directly in the compact notification.
- Replacing the validated Decision Surface.
